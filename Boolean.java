//Set of boolean functions.
public class Boolean {
    //If num1 and num2 are same return 1 else return -1 (use ternary operator)
    public int same(int num1, int num2){
        return num1==num2 ? 1:-1;
    }
}
